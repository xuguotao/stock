# 数据质量重新评估报告 (2026-07-06 13:30)

## 后续状态更新（2026-07-07）

- 健康矩阵的研究池和股票基础信息行数展示逻辑已修复，后续不应再用旧快照里的重复/物理行数判断业务覆盖。
- 腾讯 `minute/query` 1m 标准分钟线已确认只能覆盖当天，不适合历史回补；相关 `minute1_kline` 获取、存储和页面展示已下线。
- 本文中的 “等待健康检查自动更新”“2 只股票日线缺口”等结论是 2026-07-06 13:30 的状态记录，后续排查应以当前 ClickHouse 和 `/api/data/status` 返回为准。

## 问题解答

### 1. scheduled_completeness_30d 是什么？

**作用**：检查过去30个交易日内，每只股票是否有足够的日线数据（至少15个交易日）。

**逻辑**：
- 筛选非ST、非退市的股票
- 检查每只股票在最近30个交易日内有多少天的日线数据
- 如果某只股票的日线天数 < 15天，标记为"受影响股票"
- 当前状态：`ignored`（被忽略），347只股票受影响

**为什么被忽略**：这些股票可能是新上市、停牌、或交易不活跃的股票，不影响核心策略。系统选择忽略这个问题，不触发告警。

**当前状态**：✅ 正常（设计行为）

---

### 2. quote_snapshots 覆盖率 5,323/10,640 问题

**你的问题**：目标股票池没有10640只，是不是重复计算？

**答案**：✅ 是的，这是历史数据问题。

**根因**：
- 健康检查在 2026-07-06 10:39:39 运行（在我们排除北交所之前）
- 当时的 `non_st_stock_count` = 10640（包含了北交所股票）
- 现在我们排除了324只北交所股票，`non_st_stock_count` = 5323
- 但 `data_source_health` 表中存储的还是旧值 10640

**解决方案**：重新运行健康检查即可更新。

**实际覆盖率**：5,323/5,323 = 100%（最新交易日）

**当前状态**：⚠️ 需要重新运行健康检查

---

### 3. minute5 重复 24 条问题

**你的问题**：健康检查的更新机制是什么？是否需要手动重新运行？

**答案**：

**更新机制**：
- `data_source_health` 表由 `quality_snapshot` 任务自动更新
- 调度频率：每 300 秒（5分钟）一次
- 通过 `persist_clickhouse_quality_snapshot()` 函数写入

**为什么显示24条重复**：
- 健康检查在 10:39:39 运行（在我们执行 OPTIMIZE 之前）
- 我们已经在 13:10 左右执行了 `OPTIMIZE TABLE minute5_kline FINAL`
- 当前实际重复数：0
- 健康检查还没有重新运行，所以显示旧数据

**是否需要手动重新运行**：
- 不需要，系统会自动更新（每5分钟）
- 如果想立即更新，可以手动触发 `quality_snapshot` 任务

**当前状态**：✅ 已修复，等待健康检查自动更新

---

### 4. 为什么 4,992 符合条件但只有 4,990 数据就绪？

**答案**：2只股票缺少最新日线数据。

**具体股票**：
- **002729 好利科技** (SZ)
  - 最新日线：2026-06-26
  - 最新5分钟：2026-07-06
  - 问题：缺少 6/27-7/3 的日线数据（7个交易日）

- **603722 阿科力** (SH)
  - 最新日线：2026-06-26
  - 最新5分钟：2026-07-06
  - 问题：缺少 6/27-7/3 的日线数据（7个交易日）

**根因**：日线修复任务可能没有覆盖到这两只股票，或者修复失败。

**解决方案**：手动触发日线修复任务，或等待盘后维护任务自动修复。

**当前状态**：⚠️ 需要修复这2只股票的日线数据

---

## 优化后的行动方案

### Phase 1: 立即执行（低优先级，等待自动更新）

1. **重新运行健康检查**
   - 方法：等待下一个 `quality_snapshot` 周期（5分钟内）
   - 或手动触发：`python -m src.data_ops.runner --task-key quality_snapshot --once`
   - 预期结果：所有告警消失，覆盖率显示正确

### Phase 2: 修复日线缺口（中优先级）

2. **修复 002729 和 603722 的日线数据**
   ```bash
   python scripts/run_daily_maintenance.py --repair --date 2026-07-03 --symbols 002729,603722
   ```
   - 预期结果：2只股票恢复 `data_ready=1`，总数达到 4992/4992

### Phase 3: 监控（持续）

3. **监控代理错误**
   - 当前代理错误已解决（添加了 `no_proxy`）
   - 继续观察实时行情采集是否稳定
   - 如果缺失率持续 > 20%，考虑调整采集频率或优化代理配置

---

## 数据质量总览

### ✅ 优秀指标
- 股票主数据：5,534只，BJ排除完成
- 日线数据：5,303只覆盖率（最新交易日）
- 5分钟数据：5,304只覆盖率
- 数据质量：0条无效价格，0条重复
- 数据新鲜度：日线到7/3，5分钟到7/6，实时行情持续更新

### ⚠️ 待处理
- 2只股票日线缺口（002729, 603722）
- 健康检查需要重新运行（自动进行）
- 实时快照缺失率31%（代理问题，可能已解决）

### 📊 统计
- 研究标的：4,992只符合条件
- 数据就绪：4,990只（99.96%）
- 实时行情：5,323只股票，今日6,486,588条快照

---

## 建议

1. **等待健康检查自动更新**（5分钟内），观察告警是否消失
2. **盘后修复2只股票日线数据**，恢复100%数据就绪率
3. **继续监控代理配置**，确保实时行情采集稳定
4. **无需手动干预**，系统运行正常
