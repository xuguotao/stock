# 2026-07-22 项目清理计划

**目标：** 让仓库的正式入口、文档、脚本和本地运行产物与当前 `main` 的实际运行方式一致；历史内容可追溯，但不干扰日常开发。

## 范围与原则

- 先修复测试和失效入口，再整理文档与脚本。
- 已完成的设计、计划和审查文档移动到归档，不直接删除。
- 仅删除已确认无入口、与当前架构冲突的旧文件。
- 不删除 ClickHouse 数据、`data/` 本地研究数据或历史报告，除非另行确认。

## 执行项

- [x] 修复全量测试的两处合入后断言缺口。
- [x] 更新根 `README.md` 为当前 ClickHouse + Mootdx + data-ops 使用入口。
- [x] 核验 launchd 模板及其脚本入口；两者均有效，保留现状。
- [x] 将已完成的 `docs/superpowers/plans`、`specs`、`reviews` 移至 `docs/archive/2026-07-22-delivered-work/`。
- [x] 精简 `docs/README.md`，只索引当前有效文档和归档入口。
- [x] 删除无调用、依赖旧模块的 XDXR 手工同步脚本；保留仍被运行链路使用的兼容层。
- [x] 清理本地 `__pycache__`、`.DS_Store` 和可再生成的前端构建产物；不触碰业务数据。
- [x] 运行 Python 全量测试、前端构建和 Git 差异检查。

## 验收标准

- `pytest -q` 全绿；`npm run build` 成功。
- `docs/` 根目录只保留当前说明、运行手册、数据字典、当日计划和待办；历史设计位于 `archive/`。
- 不存在引用缺失脚本的受控模板。
- 旧 XDXR 手工链路不再以可执行脚本留在正式入口目录。
- Git 工作区干净，变更可按“基线修复 / 文档归档 / 脚本与本地清理”追溯。
